R2DOCX 0.5
----------------------------------------------------------------

NEW FEATURES

* MS Word bookmarks can now be replaced with R output, bookmark is now an optional argument
  of addParagraph, addPlot and addTable.

  
  
  
  
R2DOCX 0.6
----------------------------------------------------------------

NEW FEATURES

* 'addImage': add external images into a word document. Handle bookmark replacement also.
* stylename 'routput' has been added in the default docx template (EMPTY_DOC.docx), 
  it can be used as a verbatim style for text generated from the R console 
  (via capture.output for example)
